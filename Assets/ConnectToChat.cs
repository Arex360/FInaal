using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using Photon.Chat.Demo;
using Photon.Pun;
using Photon.Realtime;
public class ConnectToChat : MonoBehaviour
{
    public ChatGui gui;
    void Start()
    {
        //gui.UserName = PhotonNetwork.NickName;
        //gui.Connect();
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
